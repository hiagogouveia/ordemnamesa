# Ordem na Mesa

Sistema de checklists digitais para restaurantes.

## Stack
- Next.js 14 (App Router)
- Supabase (Auth, DB, RLS, Storage)
- Tailwind CSS
- React Query + Zustand

## Getting Started
1. Run `npm install`
2. Run `npm run dev`
